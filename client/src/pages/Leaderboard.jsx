// Leaderboard Page: Display a leaderboard with the top players based on their scores. Allow the user to enter their name and persist their score in the database after a battle.

const Leaderboard = () => {
  return <div>Leaderboard</div>;
};

export default Leaderboard;
